export type InputsTypes = {
  type: string;
  axis: string;
  series: string;
  direction: string;
};

export type chartDataTypes = {
  id: string;
  type: string;
  series: [];
  category: [];
  direction: string;
};
