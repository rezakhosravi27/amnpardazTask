export type ChartDataTypes = {
  id: string;
  chartCategory: string[];
  chartSeries: number[];
  color: string;
  direction: string;
  axis: string;
  legend: string;
  legendPosition: string;
  series: string;
  title: string;
  type: string;
};
